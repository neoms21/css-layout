import styled from "styled-components";
import { Box } from "./atoms/Box";

export const RightAligned = styled(Box)`
  align-self: flex-end;
`;

export const LeftAligned = styled(Box)`
  align-self: start;
`;
